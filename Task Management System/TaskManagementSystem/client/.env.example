# Client env (Vite)
# Copy to client/.env and adjust for your environment.

# Optional: if the frontend is hosted separately from the backend in production.
# Example: https://api.yourdomain.com
VITE_API_BASE_URL=

# Dev-only: Vite dev server will proxy /api to this target.
# Example: http://localhost:8081
VITE_API_PROXY_TARGET=http://localhost:8081
